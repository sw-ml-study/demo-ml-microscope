Use TDD to establish native mlplunit and tool-selection infrastructure, then probe current public emit_frame behavior without modifying siblings: identity return, disconnected no-op, supported numeric ranks/shapes, name and step acceptance, event order and connect/WASM payload where locally testable. Freeze the smallest recorded-observation contract from executable evidence; update the capability ledger, README, saga queue, and just check gate. Do not implement the full matrix/regression/K-means lessons or request upstream changes yet.