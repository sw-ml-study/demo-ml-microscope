Prioritize interactive viewing before K-means without modifying sibling repositories. Convert the existing MM01 and LR01 recordings into an implementation-ready Rust/Yew WASM handoff for the ../demo-extensions agent: freeze a shared fixture index/schema and budgets, specify transport/parser/reducer/view component boundaries, Previous/Next/Play/Pause/Seek/select and reduced-motion behavior, generic shape-directed rendering, exact headless/browser acceptance cases, build/deploy/run instructions, and pinned source revision. Add local validation that both fixtures satisfy the shared consumer contract and update README/docs/saga ordering. Do not implement Rust or edit ../demo-extensions.