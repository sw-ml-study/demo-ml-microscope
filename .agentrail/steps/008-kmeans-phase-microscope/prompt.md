Build the third visible microscope lesson using TDD: deterministic bounded two-cluster K-means in standalone MLPL with small fixed 2-D points and centroids. Emit literal observations for input points, initial centroids, distances or assignment evidence, labels, updated centroids, and objective across explicitly named assignment/update phases. Prove deterministic tie policy, exact or tolerant expected states, convergence, and malformed/boundary behavior. Produce a freshness-checked accessible SVG and bounded deterministic Rust/Yew fixture whose phase semantics force a general grouping decision without algorithm-specific Rust. Extend catalog/docs/checks/handoff. Do not modify siblings or implement Rust here.