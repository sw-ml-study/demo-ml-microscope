Review the matrix, linear-regression, and K-means consumers to identify the smallest genuinely domain-neutral MLPL observation helper surface. Document exact commonality and differences, decide whether extraction into ../demo-mlpl-libraries is now justified, and produce an implementation-ready sibling handoff without modifying siblings. Also document the separately actionable ../demo-extensions update to vendor the KM01 producer revision and prove it in the existing generic Yew viewer. Use TDD for any local executable contract changes and run the full gate.